/* eslint-disable no-unused-expressions */
import AppDispatcher from '../../app/dispatcher/AppDispatcher';

describe('Dispatcher', () => {
  it('should exists', () => {
    expect(AppDispatcher).toExist;
  });
});
